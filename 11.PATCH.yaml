# ---
#   - hosts: "{{server_list}}"
#     tasks:
#     - name: Patch the {{server_list}} server list
#       package:
#         name: httpd
#         state: latest
#     - name: create the file
#       file:
#         path: /tmp/{{APP_NAME}}{{ansible_data_time.time}}.txt
#         state: touch
#     - name: Fetching the output
#       debug:
#         msg:
#           - "{{ APP_NAME}}
#           - "{{ ST_DATE }}"
---
  - hosts: localhost
     tasks:
     - name: show the output
       debug:
        msg:
         - "{{ change_no }}"
         - "{{ date_time }}"
         - "{{ app_name }}"     
