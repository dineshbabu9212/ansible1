---
  - hosts: all 
    become: yes
    become_method: sudo 
    tasks: 
    - name: get the git clone from git to tower 
      local_action:
       module: git
#cline the git from git to lcl dir as its is   
       repo: 'https://github.com/dineshbabu9212/ansible1.git'
       dest: /tmp/git/
       archive: /tmp/git.zip
# clone the git as the tarball 
           
