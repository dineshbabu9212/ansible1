---
  - hosts: localhost
    # vars:
    #   CI_NAME: localhost
    #   des: "Expand the filesystem mountpoint /var on server localhost to additional disksize 4"
    tasks:

    - name: trigger from snow
      debug:
        msg:
          - description {{des}}
          - ci name is {{CI_NAME}}
          - "{{ des.split(' ')}}"
          - mount point is {{des.split(' ')[4]}}
          - requested additioal disk size {{des.split(' ')|last}}
