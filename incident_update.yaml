---
  - hosts: localhost
    gather_facts: no

    tasks:
    - name: check the incident details
      shell: |
        cat /etc/os-release
        hostname
      register: output
      delegate_to: "{{ CI_NAME }}"

    - name: Update an incident
      snow_record:
         username: dinesh12
         password: "Comnet@9212"
         instance: dev23907
         state: present
         number: "{{inc_no}}"
         table: incident
         data:
           state: "In Progress"
           assignment_group: "Server Ops Linux"
           assigned_to: "Dinesh shankar"
           work_notes: "{{output.stdout}}"
