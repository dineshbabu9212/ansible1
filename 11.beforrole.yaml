---
  - hosts: web 
    become: yes
    become_method: sudo
    vars:
     port_No: 81 
    tasks: 
    - name: install the package 
      yum: 
       name: "{{ item }}"
       state: present
      with_items:
       - httpd
       - git
       - ntp 
       - vim 
      when: ansible_os_family == "RedHat" 
      tags: install
  - name: include anoher play 
    include: 12.tempate.yaml  
